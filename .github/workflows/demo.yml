name: Demo Workflow
on: push
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get the repo code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm ci
      - name: Test app
        run: npm run test


